# Copy to .env before `docker compose up`. Both are required — compose
# interpolates them and will start with empty values if the file is missing.

# Postgres password for the searchsearcher role.
DB_PASSWORD=

# Bearer token that POST /api/ingest requires. Any feeder pushing logs or
# container inventory must send: Authorization: Bearer <this value>
INGEST_TOKEN=
